# Copy to .env and adjust if needed. Used by Prisma CLI and services.

DATABASE_URL_USER=postgresql://app:app@localhost:5433/users?schema=public
DATABASE_URL_WALLET=postgresql://app:app@localhost:5433/wallets?schema=public
